import { Model } from 'casbin/lib/cjs/model';
import { Adapter } from 'casbin/lib/cjs/persist/adapter';
export interface FilteredAdapter extends Adapter {
    loadFilteredPolicy(model: Model, filter: any): Promise<void>;
    isFiltered(): boolean;
}
